<div class="p-2 "></div>
<h1>Administration View</h1>
<div class="p-2 "></div>
<div class="container">
  <nav class="navbar navbar-expand-sm bg-primary navbar-dark">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="<%= admin_events_admin_path %>">Events</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="<%= admin_organizations_admin_path %>">Organizations</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="<%= admin_events_comments_admin_path %>">Comments</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="<%= admin_users_admin_path %>">Users</a>
      </li>
    </ul>
  </nav>
  <table class="table">
    <thead>
    <tr>
      <th>ID</th>
      <th>User Name</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Created_at</th>
      <th>Updated_at</th>
      <th> </th>
      <th> </th>
      <th> </th>
    </tr>
    </thead>
    <tbody>

    <% @users.each do |event| %>
      <tr>
        <td><%= event.id %></td>
        <td><%= event.user_name %></td>
        <td><%= event.first_name %></td>
        <td><%= event.last_name %></td>
        <td><%= event.created_at %></td>
        <td><%= event.updated_at %></td>
        <td><%= link_to "View", user_path(event.id), class: ["btn", "btn-primary"] %><td>
        <td><%= link_to "Edit", edit_user_path(event.id), class: ["btn", "btn-primary"] %><td>
        <td><%= link_to "Delete", user_path(event.id) , method: :delete, data: { confirm: 'Are you certain you want to delete this?' }, class: ["btn", "btn-primary"] %><td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>